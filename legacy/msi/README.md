# Legacy MSI Notes

This folder keeps the older WiX/MSI packaging scaffolding for reference only.

It is no longer the primary distribution path for this project.

Current active route:

- Python setup UI: `proxy_guard_setup.pyw`
- Rust helper: `proxy_guard_helper`
- Portable packaging: `packaging\build-portable.ps1`

If MSI packaging is revived in the future, these files can be used as a starting point.
